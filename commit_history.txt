name: Auto Commit Generator

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  make-commits:
    runs-on: ubuntu-latest
    steps:
      - name: 저장소 체크아웃
        uses: actions/checkout@v4

      - name: 100개 커밋 자동 생성 및 푸시
        run: |
          # 아래 두 줄의 닉네임과 이메일을 회원님의 실제 정보로 변경하세요!
          git config --global user.name "ch1rbcjf18"  
          git config --global user.email "chlrbcjf19@gmail.com"
          
          for i in {1..100}
          do
            echo "Final real commit number $i" >> final_history.txt
            git add final_history.txt
            git commit -m "Final verification commit $i"
          done
          
          git push
Auto commit number 1
Auto commit number 2
Auto commit number 3
Auto commit number 4
Auto commit number 5
Auto commit number 6
Auto commit number 7
Auto commit number 8
Auto commit number 9
Auto commit number 10
Auto commit number 11
Auto commit number 12
